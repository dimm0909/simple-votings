function change_voting() {
    save_vote.hidden = !save_vote.hidden
    change_vote.hidden = !change_vote.hidden
    voting_name.hidden = !voting_name.hidden
    voting_description.hidden = !voting_description.hidden
    voting_name_input.hidden = !voting_name_input.hidden
    voting_description_input.hidden = !voting_description_input.hidden
}

function add_varinant() {
    form_add_var.hidden = !form_add_var.hidden
    save_new_var.hidden = !save_new_var.hidden
    add_variant.hidden = !add_variant.hidden
}
